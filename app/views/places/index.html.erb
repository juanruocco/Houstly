<h1>HOTELES!</h1>
<table>
  <p><%=current_user.first_name %></p>
  <%#=@current_user.first_name%>
  <%= render "form" %>

  <%
  places = current_user.places
  #places = Place.all	#master
  #puts "places user"	#master
  #puts @current_user.places.all.count	#master
  places.each do |place|
 %>

  <tr>
    <td>
      <%= link_to place.name_id, edit_user_place_path(current_user, place), class: "btn btn-default" %>
    </td>
    <td><%= place.name %></td>
    <td><%= place.description %></td>
    <td><%= place.facebook_url %></td>
    <td><%= place.email %></td>
    <td><%= place.cellphone %></td>
    <td nowrap><%= place.logo_path %></td>
    <td><%= place.location_url %></td>
    <td><%= place.location_name %></td>
    <td><%= place.video_url %></td>
    <td><%= place.reservar_url %></td>
    <td><%= place.music_path %></td>
    <td><%= place.max_slider_images %></td>
    <td><%= place.max_thumb_images %></td>
    <td><%= place.direction %></td>
    <td><%= place.telephone %></td>
    <td>
      <%= button_to "Delete", user_place_path(current_user, place), method: :delete, data: { confirm: "Are you sure you wish to delete #{place.name_id} - #{place.name}?" }, class: "btn btn-danger" %>
    </td>

    <td>
      <% if place.errors.any? %>
         <h2>
           <%= pluralize( userit.errors.count, "error") %> :
           <ul>
             <% place.errors.full_messages.each do |msj| %>
              <li><%= msj %></li>
             <%
             end
             %>
           </ul>
         </h2>
      <%
      end
      %>
    </td>
  </tr>
  <% end %>
</table>
